{
  "name": "dots-game",
  "version": "0.1.0",
  "private": true,
  "description": "Online dots enclosure game without accounts.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check game.js && node --check rules.js"
  },
  "engines": {
    "node": ">=18"
  }
}
